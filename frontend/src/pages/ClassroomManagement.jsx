import { useCallback, useEffect, useState } from 'react';
import AppShell from '../components/AppShell';
import { archiveClassroom, createClassroom, listClassrooms, updateClassroom } from '../api/classrooms';

const blank = { name: '', fullName: '', ageGroup: '3-4', maxSize: 25, schoolYear: '2026-2027' };
export default function ClassroomManagement() {
  const role = JSON.parse(localStorage.getItem('user') || '{}').role; const canManage = ['admin', 'principal'].includes(role);
  const [items, setItems] = useState([]); const [form, setForm] = useState(blank); const [editingId, setEditingId] = useState(null); const [showArchived, setShowArchived] = useState(false); const [loading, setLoading] = useState(true); const [saving, setSaving] = useState(false); const [error, setError] = useState('');
  const load = useCallback(async () => { setLoading(true); try { const response = await listClassrooms(showArchived); setItems(response.data.data || []); } catch (err) { setError(err.response?.data?.message || 'Không thể tải danh sách lớp.'); } finally { setLoading(false); } }, [showArchived]);
  useEffect(() => { void Promise.resolve().then(load); }, [load]);
  const set = (key, value) => setForm((current) => ({ ...current, [key]: value }));
  const submit = async (event) => { event.preventDefault(); setSaving(true); setError(''); try { if (editingId) await updateClassroom(editingId, form); else await createClassroom(form); setForm(blank); setEditingId(null); await load(); } catch (err) { setError(err.response?.data?.message || 'Không thể lưu lớp học.'); } finally { setSaving(false); } };
  const edit = (item) => { setEditingId(item._id); setForm({ name: item.name, fullName: item.fullName || '', ageGroup: item.ageGroup, maxSize: item.maxSize, schoolYear: item.schoolYear }); window.scrollTo({ top: 0, behavior: 'smooth' }); };
  const archive = async (item) => { if (!window.confirm(`Lưu trữ lớp ${item.name}?`)) return; try { await archiveClassroom(item._id); await load(); } catch (err) { setError(err.response?.data?.message || 'Không thể lưu trữ lớp học.'); } };
  return <AppShell title="Lớp học" subtitle="Quản lý danh sách lớp, sĩ số và trạng thái hoạt động."><div className="classroom-layout">{canManage && <form className="content-card classroom-form" onSubmit={submit}><div className="card-heading"><div><p className="card-kicker">{editingId ? 'CẬP NHẬT LỚP' : 'TẠO LỚP MỚI'}</p><h2>{editingId ? `Sửa ${form.name}` : 'Thông tin lớp học'}</h2></div></div>{error && <p className="form-error">{error}</p>}<label>Tên lớp<input required maxLength="80" value={form.name} onChange={(e) => set('name', e.target.value)} placeholder="Ví dụ: MGL 4-5 A" /></label><label>Tên đầy đủ<input maxLength="120" value={form.fullName} onChange={(e) => set('fullName', e.target.value)} placeholder="Ví dụ: Mẫu giáo lớn 4–5 tuổi, lớp A" /></label><div className="classroom-form-row"><label>Nhóm tuổi<select value={form.ageGroup} onChange={(e) => set('ageGroup', e.target.value)}><option value="3-4">3–4 tuổi</option><option value="4-5">4–5 tuổi</option><option value="5-6">5–6 tuổi</option></select></label><label>Sĩ số tối đa<input required type="number" min="1" max="60" value={form.maxSize} onChange={(e) => set('maxSize', e.target.value)} /></label></div><label>Năm học<input required pattern="\d{4}-\d{4}" value={form.schoolYear} onChange={(e) => set('schoolYear', e.target.value)} placeholder="2026-2027" /></label><div className="form-actions">{editingId && <button className="button button-secondary" type="button" onClick={() => { setEditingId(null); setForm(blank); }}>Hủy sửa</button>}<button className="button button-primary" disabled={saving}>{saving ? 'Đang lưu...' : editingId ? 'Lưu thay đổi' : 'Tạo lớp học'}</button></div></form>}<section className="content-card classroom-list"><div className="card-heading"><div><p className="card-kicker">DANH SÁCH LỚP</p><h2>{items.length} lớp học</h2></div><label className="archive-filter"><input type="checkbox" checked={showArchived} onChange={(e) => setShowArchived(e.target.checked)} />Hiện cả lớp đã lưu trữ</label></div>{loading ? <div className="empty-state">Đang tải lớp học...</div> : <div className="student-table-wrap"><table className="data-table"><thead><tr><th>Lớp</th><th>Độ tuổi</th><th>Năm học</th><th>Sĩ số</th><th>Trạng thái</th>{canManage && <th>Thao tác</th>}</tr></thead><tbody>{items.map((item) => <tr key={item._id}><td><strong>{item.name}</strong><small>{item.fullName || '—'}</small></td><td>{item.ageGroup} tuổi</td><td>{item.schoolYear}</td><td>{item.statistics?.currentStudents || 0} / {item.maxSize}</td><td><span className={`student-status ${item.status === 'archived' ? 'withdrawn' : 'enrolled'}`}>{item.status === 'archived' ? 'Đã lưu trữ' : 'Đang hoạt động'}</span></td>{canManage && <td><div className="classroom-actions"><button className="button button-secondary" onClick={() => edit(item)} disabled={item.status === 'archived'}>Sửa</button><button className="button button-danger" onClick={() => archive(item)} disabled={item.status === 'archived'}>Lưu trữ</button></div></td>}</tr>)}</tbody></table></div>}{!loading && !items.length && <div className="empty-state"><strong>Chưa có lớp học</strong></div>}</section></div></AppShell>;
}
