import { useEffect, useState } from 'react';
import { useNavigate, useParams } from 'react-router-dom';
import AppShell from '../components/AppShell';
import { createStudent, getStudent, updateStudent } from '../api/students';
import api from '../api/axiosConfig';

const empty = { fullName: '', birthDate: '', gender: 'male', classroomId: '', admissionDate: '', schoolYear: '2026-2027', address: '', nationality: 'Việt Nam', ethnicity: '', birthPlace: '', notes: '', allergyText: '', diseaseName: '', diseaseDescription: '', emergencyName: '', emergencyPhone: '' };
const toDate = (value) => value ? new Date(value).toISOString().slice(0, 10) : '';

export default function StudentForm() {
  const { id } = useParams();
  const navigate = useNavigate();
  const [form, setForm] = useState(empty); const [classes, setClasses] = useState([]); const [saving, setSaving] = useState(false); const [error, setError] = useState('');
  const editing = Boolean(id); const role = JSON.parse(localStorage.getItem('user') || '{}').role; const manager = ['admin', 'principal'].includes(role);
  useEffect(() => { Promise.all([api.get('/classrooms'), editing ? getStudent(id) : Promise.resolve(null)]).then(([classRes, studentRes]) => { setClasses(classRes.data.data || []); if (studentRes) { const data = studentRes.data.data; setForm((old) => ({ ...old, ...data, birthDate: toDate(data.birthDate), admissionDate: toDate(data.admissionDate), allergyText: (data.allergies || []).map((item) => item.allergen).join(', '), diseaseName: data.disease?.name || '', diseaseDescription: data.disease?.description || '', emergencyName: data.emergencyContact?.name || '', emergencyPhone: data.emergencyContact?.phone || '' })); } }).catch(() => setError('Không thể tải dữ liệu biểu mẫu.')); }, [id, editing]);
  const set = (key, value) => setForm((old) => ({ ...old, [key]: value }));
  const submit = async (event) => { event.preventDefault(); setSaving(true); setError(''); try { const teacherFields = ['fullName', 'birthDate', 'gender', 'address', 'nationality', 'ethnicity', 'birthPlace', 'avatar', 'notes']; const managerFields = [...teacherFields, 'classroomId', 'admissionDate', 'schoolYear']; const data = Object.fromEntries((manager ? managerFields : teacherFields).filter((key) => Object.hasOwn(form, key)).map((key) => [key, form[key]])); if (manager) { data.allergies = form.allergyText.split(',').map((item) => item.trim()).filter(Boolean).map((allergen) => ({ allergen, severity: 'moderate' })); data.disease = { name: form.diseaseName.trim(), description: form.diseaseDescription.trim() }; data.emergencyContact = { name: form.emergencyName.trim(), phone: form.emergencyPhone.trim() }; } if (editing) await updateStudent(id, data); else await createStudent(data); navigate(editing ? `/students/${id}` : '/students'); } catch (err) { setError(err.response?.data?.message || 'Không thể lưu hồ sơ học sinh.'); } finally { setSaving(false); } };
  return <AppShell title={editing ? 'Cập nhật học sinh' : 'Thêm học sinh'} subtitle="Thông tin hồ sơ được kiểm tra và phân quyền tại máy chủ."><form className="content-card student-form" onSubmit={submit}><div className="section-heading"><div><p className="card-kicker">HỒ SƠ HỌC SINH</p><h2>{editing ? 'Cập nhật thông tin' : 'Tạo hồ sơ mới'}</h2></div></div>{error && <p className="form-error">{error}</p>}<div className="student-form-grid"><label>Họ và tên<input required maxLength="120" value={form.fullName} onChange={(e) => set('fullName', e.target.value)} /></label><label>Ngày sinh<input required type="date" value={form.birthDate} onChange={(e) => set('birthDate', e.target.value)} /></label><label>Giới tính<select value={form.gender} onChange={(e) => set('gender', e.target.value)}><option value="male">Nam</option><option value="female">Nữ</option></select></label><label>Lớp học<select required value={form.classroomId} onChange={(e) => set('classroomId', e.target.value)} disabled={editing && !manager}><option value="">Chọn lớp học</option>{classes.map((item) => <option key={item._id} value={item._id}>{item.name}</option>)}</select></label>{manager && <><label>Ngày nhập học<input type="date" value={form.admissionDate} onChange={(e) => set('admissionDate', e.target.value)} /></label><label>Năm học<input value={form.schoolYear} onChange={(e) => set('schoolYear', e.target.value)} placeholder="Ví dụ: 2026-2027" /></label></>}<label className="form-wide">Địa chỉ<input maxLength="500" value={form.address || ''} onChange={(e) => set('address', e.target.value)} /></label>{manager && <><label>Quốc tịch<input value={form.nationality || ''} onChange={(e) => set('nationality', e.target.value)} /></label><label>Nơi sinh<input value={form.birthPlace || ''} onChange={(e) => set('birthPlace', e.target.value)} /></label><label className="form-wide">Dị ứng / lưu ý ăn uống<textarea value={form.allergyText} onChange={(e) => set('allergyText', e.target.value)} placeholder="Nhập từng dị nguyên, ngăn cách bằng dấu phẩy. Ví dụ: tôm, sữa bò" /><small>Hiển thị cho bộ phận chuyên môn và bếp ăn.</small></label><label>Bệnh lý cần lưu ý<input value={form.diseaseName} onChange={(e) => set('diseaseName', e.target.value)} placeholder="Ví dụ: hen suyễn" /></label><label>Mô tả / hướng dẫn y tế<input value={form.diseaseDescription} onChange={(e) => set('diseaseDescription', e.target.value)} placeholder="Ví dụ: mang thuốc xịt theo chỉ định" /></label><label>Người liên hệ khẩn cấp<input value={form.emergencyName} onChange={(e) => set('emergencyName', e.target.value)} /></label><label>Số điện thoại khẩn cấp<input value={form.emergencyPhone} onChange={(e) => set('emergencyPhone', e.target.value)} /></label></>}<label className="form-wide">Ghi chú<textarea maxLength="2000" value={form.notes || ''} onChange={(e) => set('notes', e.target.value)} /></label></div><div className="form-actions"><button type="button" className="button button-secondary" onClick={() => navigate(-1)}>Hủy</button><button className="button button-primary" disabled={saving}>{saving ? 'Đang lưu...' : 'Lưu hồ sơ'}</button></div></form></AppShell>;
}
